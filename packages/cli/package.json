{
  "name": "@usereq/bundlehive-cli",
  "version": "0.1.0",
  "description": "The `bundlehive` CLI — dev server and build for embeddable widgets.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "bundlehive": "./dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@usereq/bundlehive-build": "workspace:*",
    "vite": "^8.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
